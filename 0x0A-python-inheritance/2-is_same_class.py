#!/usr/bin/python3
"""
module is good
"""


def is_same_class(obj, a_class):
    """
    function module to return obj and it class
    """
    if type(obj) == a_class:
        return True
    else:
        return False
